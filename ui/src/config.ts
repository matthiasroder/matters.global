// Configuration for the chatbot UI

// WebSocket endpoint for the matters.global backend
export const WEBSOCKET_ENDPOINT = "ws://localhost:8091";

// Application settings
export const APP_SETTINGS = {
  title: "Matters.Global Problem Assistant",
  description: "A conversational interface for managing problems and their solutions",
  theme: {
    light: "light",
    dark: "dark",
  }
};